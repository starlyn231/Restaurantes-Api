import { RestaurantePipePipe } from './restaurante-pipe.pipe';

describe('RestaurantePipePipe', () => {
  it('create an instance', () => {
    const pipe = new RestaurantePipePipe();
    expect(pipe).toBeTruthy();
  });
});
